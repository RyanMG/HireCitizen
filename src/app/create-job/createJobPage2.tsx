import { FormData } from "@/types/Forms";
export default function CreateJobPage2({
  returnFormData,
  formData
}: {
  returnFormData: (formData: any) => void,
  formData: FormData
}) {
  return (
    <div>
      <h1>Create Job Page 2</h1>
    </div>
  );
}
